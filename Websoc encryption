const encryptedMessage =
await encryptMessage(
    text,
    receiverPublicKey
);


socket.send(

    JSON.stringify({

        type:"message",

        receiver:selectedUser,

        message:encryptedMessage

    })

);
